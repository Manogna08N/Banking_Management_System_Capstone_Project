spring.application.name=payment-service
serve=8089
spring.datasource.url=jdbc:mysql://localhost:3306/payment_db
spring.datasource.password=Manogna@2003

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==================================================
# JPA / Hibernate Settings
# ==================================================
spring.jpa.hibernate.ddl-auto=update   # options: update, create, create-drop, validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ==================================================
# Custom Application Properties
# ==================================================
# Used in PaymentController to simulate random failures
payments.simulate-failure=false

# ==================================================
# Logging
# ==================================================
logging.level.org.springframework=INFO
logging.level.com.example.payment=DEBUG